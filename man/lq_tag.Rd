% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lq.R
\name{lq_tag}
\alias{lq_tag}
\title{Tag a specific revision of a template}
\usage{
lq_tag(path, name, tag, rev = NULL, force = FALSE)
}
\arguments{
\item{path}{Path to the store.}

\item{name}{Template name.}

\item{tag}{Tag name. Can't be \code{"HEAD"} (any casing) or look like a commit
id — both would be ambiguous as a revision.}

\item{rev}{Revision to tag instead of the current \code{HEAD}.}

\item{force}{Overwrite the tag if it already exists.}
}
\value{
Invisibly, a \code{lenguar_tag_result} (has \verb{$name}, \verb{$tag}, \verb{$commit}).
}
\description{
Points a named tag at \code{name}'s current revision, or at \code{rev} — this is
how you retroactively tag a \emph{prior} revision, e.g. tagging the version
before your latest edit. These are lengua's own tags
(\verb{refs/lengua/tags/<name>/<tag>}), not git tags: they're scoped per
template, so the same tag name can exist independently on several
templates. A tag name works anywhere \code{\link[=lq_get]{lq_get()}}'s \code{rev} or \code{\link[=lq_diff]{lq_diff()}}'s
\code{from}/\code{to} accept a revision.
}
\examples{
store <- withr::local_tempdir()
lq_init(store)
lq_add(store, "letter.md", "We had so much fun.", message = "past tense")
lq_add(store, "letter.md", "We will have so much fun.", message = "future tense")
lq_tag(store, "letter.md", "tense-future")
lq_tag(store, "letter.md", "tense-past", rev = "HEAD~1")
}
