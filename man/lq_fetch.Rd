% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lq.R
\name{lq_fetch}
\alias{lq_fetch}
\title{Add another source to an already-initialized library}
\usage{
lq_fetch(
  path,
  name = NULL,
  from_dir = NULL,
  from_repo = NULL,
  ref = NULL,
  subdir = NULL,
  force = FALSE
)
}
\arguments{
\item{path}{Path to the library (as created by \code{\link[=lq_init]{lq_init()}}).}

\item{name}{Name for the new source. Auto-derived from \code{from_dir}'s
basename or \code{from_repo}'s last path segment if omitted — errors asking
for an explicit \code{name} on a collision, rather than silently picking a
different one.}

\item{from_dir, from_repo, ref, subdir, force}{See \code{\link[=lq_init]{lq_init()}}.}
}
\value{
Invisibly, a \code{lenguar_fetch_result} (has \verb{$source} and
\verb{$warnings}, a character vector, possibly empty).
}
\description{
Takes the same adoption arguments as \code{\link[=lq_init]{lq_init()}}, but requires \code{path} to
already be a library and appends rather than replaces — this is how a
project pools templates from more than one existing store. The new
source becomes the highest-precedence one for merged reads: if it
defines a template name that another source already has, that name now
resolves to the new source (see \code{\link[=lq_get]{lq_get()}}/\code{\link[=lq_list]{lq_list()}}/\code{\link[=lq_search]{lq_search()}}),
and the returned result's \code{warnings} names any such collision.
}
\examples{
store <- withr::local_tempdir()
lq_init(store)
other <- withr::local_tempdir()
lq_init(other)
lq_fetch(store, from_dir = other, name = "other")
}
