% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lq.R
\name{lq_update}
\alias{lq_update}
\title{Refresh one or every source in a library}
\usage{
lq_update(path, source = NULL)
}
\arguments{
\item{path}{Path to the library.}

\item{source}{Refresh only this source. Defaults to every source.}
}
\value{
A \code{lenguar_update} data frame with \code{source}, \code{status}
(\code{"up-to-date"}, \code{"fast-forwarded"}, \code{"not-updatable"}, or \code{"error"}),
and \code{detail} columns. Never stops at the first per-source failure when
refreshing every source.
}
\description{
Fetches from each source's recorded origin and fast-forwards to it —
never a hard reset, so a source that has locally diverged from its
origin fails loudly (reported as \code{status = "error"} in the returned data
frame) instead of silently losing anything. A \code{local} source, or one
imported with \code{subdir} (its history/tags weren't preserved on import, so
there's nothing to fast-forward against), always reports
\code{status = "not-updatable"} — informational, not an error. There's no
separate lockfile: each source's own git history is the record of what
commit it's at.
}
\examples{
store <- withr::local_tempdir()
lq_init(store)
lq_update(store)
}
